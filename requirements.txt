# Core pipeline
PyYAML>=6.0.2
numpy>=1.26
packaging>=24.0

# Hugging Face / training stack
# Install the CUDA build of torch that matches your system first if needed, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.8.0
transformers>=4.52.0
accelerate>=1.8.0
datasets>=3.1.0
trl>=0.19.0
peft>=0.17.0
safetensors>=0.4.5
sentencepiece>=0.2.0
protobuf>=5.0

# Quantization / memory-efficient loading
bitsandbytes>=0.46.0
gptqmodel>=7.0.0

# Judge server/client
vllm>=0.10.0
openai>=1.99.0
